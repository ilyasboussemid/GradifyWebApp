server:
  port: 8082
spring:
  application:
    name: student-service
sparql:
  endpoint: ${SPARQL_ENDPOINT:http://localhost:3030/lod/sparql}
management:
  endpoints:
    web:
      exposure:
        include: health,info
