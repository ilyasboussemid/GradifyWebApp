server:
  port: 8081
spring:
  application:
    name: offer-service
sparql:
  endpoint: ${SPARQL_ENDPOINT:http://localhost:3030/lod/sparql}
management:
  endpoints:
    web:
      exposure:
        include: health,info
