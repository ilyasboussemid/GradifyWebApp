package com.gradify.gateway.config;

import org.springframework.context.annotation.Configuration;

/**
 * Configuration des routes — gérée via application.yml.
 * Cette classe peut être étendue pour ajouter des routes programmatiques.
 */
@Configuration
public class RouteConfig {
    // Routes defined in application.yml
    // Extend here for programmatic route definitions if needed
}
